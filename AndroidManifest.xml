<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.fssle.adpad"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:label="@string/app_name"
        android:icon="@drawable/icon"
        android:theme="@style/Theme.AdPad"
        android:name=".AdPadApplication">
        
        <uses-library android:name="com.google.android.maps" />
        
        <activity android:name=".AdPadActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>  
        
        <activity android:name=".AdBannerActivity" />
        <activity android:name=".NewsActivity" />
		    <activity android:name=".MapActivity" />
        <activity android:name=".EventsActivity" />
        <activity android:name=".ScheduleActivity" /> 
        <activity android:name=".RidersActivity" />
		    <activity android:name=".PartnersActivity" />
        <activity android:name=".ShareActivity" />
        <activity android:name=".SocialActivity" />    
        
    </application> 
    
    <uses-permission android:name="android.permission.INTERNET" />
    	
</manifest> 
